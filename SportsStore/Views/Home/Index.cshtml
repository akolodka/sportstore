@model IQueryable<Product>

@foreach (var p in Model 

    // В контроллер можно передать View без модели, и получить Null
    ?? Enumerable.Empty<Product>()){

    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}